# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# MLflow tracking store + artifacts
mlflow.db
mlruns/

# Generated model artifacts (regenerated by run_all.sh)
models/*.pkl
models/*.json

# Generated data + reports (keep folders via .gitkeep, ignore contents)
data/processed/*.csv
data/staging/
data/intermediate/
data/marts/
data/feature_store/
reports/monitoring/*
reports/*.json
reports/*.csv

# Claude / project meta (local tooling, not for version control)
CLAUDE.md
TASKS.md
lessons_learned.md
IMPLEMENTATION_PLAN.md

# Secrets — never commit
.env

# OS
.DS_Store
powermetrics_log.txt

# NOTE: data/raw/student_performance.csv IS committed (the input dataset).
!data/raw/student_performance.csv
.cursor/rules/codegraph.mdc
.codegraph/.gitignore
.codegraph/daemon.pid
.codegraph/codegraph.lock
